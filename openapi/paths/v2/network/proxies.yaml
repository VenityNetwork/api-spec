get:
  tags:
    - Network
  summary: Get information about all proxies
  responses:
    200:
      description: List of proxies
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../../../../openapi.yaml#/components/schemas/ProxyInfo'
