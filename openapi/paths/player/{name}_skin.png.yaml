get:
  tags:
    - Legacy API
  summary: Get player skin by name
  parameters:
    - name: name
      in: path
      required: true
      schema:
        type: string
  responses:
    200:
      description: Player skin image
      content:
        image/png:
          schema:
            type: string
            format: binary

